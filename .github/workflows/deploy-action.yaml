name: Deploy
on: push
  jobs:
    deploy:
      runs-on: ubuntu-latest
      steps:
        - name: Checkout code
          uses: actions/checkout@v2
           
        - name: lint
          run: |
            echo "Running linting..." 
            npm run lint
        - name: Run tests
          run: |
            echo "Running tests..."
            npm test
        - name: Build project
          run: |
            echo "Building project..."
            npm run build
        - name: deploy
          run: |
            echo "Deploying project..." 
            # Add your linting commands here
        - name: Deploy to production
          run: |
            echo "Deploying to production..."
            # Add your deployment commands here   
  
      - name: Notify team after deployment      
        run: |
          echo "Deployment successful! Notifying team..."
          # Add your notification commands here (e.g., send an email, post to Slack, etc.)